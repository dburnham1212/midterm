<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">


  <title>Create Quiz</title>

  <!-- Include global styles-->
  <%- include('partials/_global_styling') %>
  <!--Local Styling -->
  <link rel="stylesheet" href="/styles/submitQuiz.css" />

  <script src="/vendor/jquery-3.0.0.js"></script>
</head>
<body>
  <%- include('partials/_header') %>
  <!-- middle -->
  <main>
    <div class ="submit-card">
      <h1> QUIZ COMPLETE! </h1>
      <p> Quiz name: <%= quiz.title %></p>
      <p> Best Score: <%=result.highest_score %> / <%=result.out_of %></p>
      <p> Last Score: <%=result.last_score %> / <%=result.out_of %></p>
      <form action="/submitQuiz/submitReview/<%= quiz.id %>" method="POST">
        <div class="favourite-container">
          <p>Favourited</p>
          <input type="checkbox" id="fav-button" name="favourite" checked="<%= result.favourited %>"></input>

          <p> Rating </p>
          <select name="rating">
            <option value="1">1</option>
            <option value="2">2</option>
            <option value="3">3</option>
            <option value="4">4</option>
            <option value="5">5</option>
          </select>
        </div>
        <button class="submit-button form-button"> done </button>
      </form>

    </div>
  </main>

  <!-- footer -->
  <div>

  </div>
</body>
</html>
